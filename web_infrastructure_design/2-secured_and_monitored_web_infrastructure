You must be able to explain some specifics about this infrastructure:

For every additional element, why you are adding it

What are firewalls for : 
- to have control over income and outcome trafic with security

Why is the traffic served over HTTPS
- to have some encryption on data send and request

What monitoring is used for
- it is used to send the error detected on the server and it is doing data metric

How the monitoring tool is collecting data
- it is using protocol HTTP, window log event, and more

Explain what to do if you want to monitor your web server QPS
- you install the monitoring agent on each server to track the metrics and the downtime

You must be able to explain what the issues are with this infrastructure:

Why terminating SSL at the load balancer level is an issue
- the rest of the data cannot be encrypted

Why having only one MySQL server capable of accepting writes is an issue
- problem for backup storage and for the request so you do all the CRUD on only one database in spite of 2 to do the insert update delete on the primary and one replica to do the read request.

Why having servers with all the same components (database, web server and application server) might be a problem
- Don't check how the request are treated for the algorithm of round robin
